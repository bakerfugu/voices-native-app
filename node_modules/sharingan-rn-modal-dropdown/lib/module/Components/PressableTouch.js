import React from 'react';
import { Pressable, Platform } from 'react-native';
import styles from '../styles';

const PressableTouch = props => {
  const {
    rippleColor,
    disabled,
    onPress,
    children
  } = props;
  return /*#__PURE__*/React.createElement(Pressable, {
    onPress: onPress,
    disabled: disabled,
    style: ({
      pressed
    }) => [{
      backgroundColor: pressed && Platform.OS === 'ios' ? rippleColor : 'transparent'
    }, styles.rippleStyle],
    android_ripple: {
      color: rippleColor
    }
  }, children);
};

export default PressableTouch;
//# sourceMappingURL=PressableTouch.js.map